const jwt = require("jsonwebtoken");
const bcrypt = require("bcrypt");

const register = () => {};

const login = () => {};

const logout = () => {};

module.exports = {
  register,
  login,
  logout,
};
